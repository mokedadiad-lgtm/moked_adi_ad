/**
 * Placeholder for WhatsApp interactive `body.text` when we only want quick-reply buttons visible.
 * Meta requires body text; clients usually render this as empty space.
 */
export const WA_INTERACTIVE_BODY_BUTTONS_ONLY = "\u200c";
