# WHATSAPP BOT (INBOUND) - OFFICIAL TEXTS & BUTTONS

מטרת המסמך: סט תבניות רשמי לבוט הוואטסאפ (FSM) כולל טקסטים, וכפתורים בכל שלב.
המסמך מיועד לעריכה ידנית ע"י הצוות; **טקסטי הודעות** נטענים מהקובץ הזה בקוד (`lib/whatsapp/botTexts.ts` + `botFsm.ts`).

> **חשוב — כפתורי Quick Reply בוואטסאפ (Meta):** לכל כפתור מותרת **תווית של עד 20 תווים**. לכן תחת כל שלב יש שני דברים אפשריים:
> - **טקסט** — מה שהמשתמש רואה בגוף ההודעה (אין מגבלה קצרה כזו).
> - **כפתורים (בפועל בוואטסאפ)** — התוויות המדויקות שנשלחות ל־API (חייבות להישאר ≤20 תווים). אם תשנו אותן כאן, יש לעדכן גם את `WA_REPLY_*` ב־`lib/whatsapp/botFsm.ts` כדי שה־`id`ים והלוגיקה יישארו תואמים.

## משתנים (להחלפה בתוך הטקסטים)
- `{{asker_gender}}` -> `זכר` / `נקבה`
- `{{asker_age}}` -> גיל כמספר
- `{{title}}` -> כותרת
- `{{content}}` -> תוכן השאלה (טקסט מלא, כולל שורות)
- `{{response_type}}` -> `short` / `detailed` (או קצר/מורחב לפי מה שתבחרו להציג)
- `{{publication_consent}}` -> `publish` / `blur` / `none`
- `{{delivery_preference}}` -> `whatsapp` / `email` / `both`
- `{{asker_email}}` -> אימייל אם נאסף

## UX כללי (אופציונלי)
- קלט שלא מתאים לכפתורים: להחזיר הודעת תיקון קצרה + להציג כפתורים של אותו שלב.
- תוכן שאלה בכמה הודעות: לצבור ב-`context.body_parts[]` ואז בסיום לשמור כטקסט רציף (למשל join עם שורה חדשה).
- "עד 4 שינויים": הכוונה למספר פעולות עריכה (גם אם אותו שדה), כפי שסוכם.

---

## State: start
**טקסט**
M: ברוכים הבאים לבוט של 'אסק מי פלוס' מבית עדי-עד!

המספר שממנו שלחת את ההודעה מזוהה לצורך שליחת המענה לפניתך.
יש לבחור את האפשרות הנכונה עבורך כדי שנמשיך.

F: ברוכים הבאים לבוט של 'אסק מי פלוס' מבית עדי-עד!

המספר שממנו שלחת את ההודעה מזוהה לצורך שליחת המענה לפניתך.
יש לבחור את האפשרות הנכונה עבורך כדי שנמשיך.

**כפתורים (בפועל בוואטסאפ)**
- זכר
- נקבה

---

## State: gender_invalid_first (fallback לקלט לא תקין)
**טקסט**
M: לא זיהיתי בחירה תקינה.
נא לבחור את האפשרות הרצויה באמצעות הכפתורים: `זכר` או `נקבה`.

F: לא זיהיתי בחירה תקינה.
נא לבחור את האפשרות הרצויה באמצעות הכפתורים: `זכר` או `נקבה`.

**כפתורים (בפועל בוואטסאפ)**
- זכר
- נקבה

---

## State: choose_mode
**טקסט**
M: תודה.
אתה מעוניין בפניה ושאלה במוקד או להשאיר הודעה לנציג אנושי?

F: תודה.
את מעוניינת בפניה ושאלה במוקד או להשאיר הודעה לנציג אנושי?

**כפתורים (בפועל בוואטסאפ)** — תווית קצרה; ההסבר המלא בגוף ההודעה למעלה.
- שאלה במוקד (`MODE_BOT`)
- נציג אנושי (`MODE_HUMAN`)

---

## State: human_message_collect (כתיבת הודעה לנציג אנושי)
**טקסט**
M: נא כתוב את הודעתך לנציג האנושי.
לאחר שתשלח הודעה ראשונה יופיעו כפתורים: `המשך עוד` או `סיימתי`.

F: נא כתבי את הודעתך לנציגת האנושי.
לאחר שתשלחי הודעה ראשונה יופיעו כפתורים: `המשך עוד` או `סיימתי`.

**כפתורים (בפועל בוואטסאפ)** — הכפתורים לא נשלחים מיד עם ההנחיה; הם מופיעים אחרי ההודעה הראשונה של המשתמש.
- המשך עוד (`HUMAN_ADD_MORE`)
- סיימתי (`HUMAN_DONE`)

---

## State: human_handoff
**טקסט**
M: פנייתך הועברה למענה אנושי.
ניצור עמך קשר בהקדם האפשרי.

F: פנייתך הועברה למענה אנושי.
ניצור עמך קשר בהקדם האפשרי.

---

## State: age
**טקסט**
M: נא להזין את הגיל שלך באמצעות מספרים בלבד.

F: נא להזין את הגיל שלך באמצעות מספרים בלבד.

---

## State: age_invalid_first
**טקסט**
M: הגיל שהוזן אינו בטווח הרלוונטי או שאינו מספר.
נא להזין את גילך.

F: הגיל שהוזן אינו בטווח הרלוונטי או שאינו מספר.
נא להזין את גילך.

---

## State: age_invalid_second (סיום קשיח)
**טקסט**
M: השירות אינו מיועד לפונה בגילך.
תודה על פנייתך. השיחה תיסגר כעת.

*(ללא כפתורים)*

F: השירות אינו מיועד לפונה בגילך.
תודה על פנייתך. השיחה תיסגר כעת.

*(ללא כפתורים)*

---

## State: body_collect
**טקסט**
M: כעת נא לכתוב את השאלה שלך.
ניתן לשלוח את התוכן במספר הודעות.
לאחר שתשלח את התוכן הראשון - יופיעו כפתורים לסיום או המשך.

F: כעת נא לכתוב את השאלה שלך.
ניתן לשלוח את התוכן במספר הודעות.
לאחר שתשלחי את התוכן הראשון - יופיעו כפתורים לסיום או המשך.

**כפתורים (בפועל בוואטסאפ)**
- להוסיף עוד
- סיימתי

---

## State: body_invalid_or_empty
**טקסט**
M: לא נקלט תוכן שאלה תקין.
נא לכתוב את השאלה באופן ברור.

F: לא נקלט תוכן שאלה תקין.
נא לכתוב את השאלה באופן ברור.

**כפתורים (בפועל בוואטסאפ)**
- להוסיף עוד
- סיימתי

---

## State: title_collect
**טקסט**
M: נא להזין כותרת קצרה לשאלה.

לאחר שתשלח את הכותרת הראשונה - יופיעו כפתורים לסיום או הוספה.

F: נא להזין כותרת קצרה לשאלה.

לאחר שתשלחי את הכותרת הראשונה - יופיעו כפתורים לסיום או הוספה.

**כפתורים (בפועל בוואטסאפ)**
- הוסף עוד
- סיימתי

---

## State: response_type
**טקסט**
M: באיזו רמת פירוט תרצה לקבל מענה?

F: באיזו רמת פירוט תרצי לקבל מענה?

**כפתורים (בפועל בוואטסאפ)** — בפועל קצרים; ב**סיכום האישור** מוצג הניסוח המלא דרך `{{response_type}}` (ראו משתנים למעלה / קוד).
- קצר ולעניין (`RESP_SHORT`)
- מורחב (`RESP_DETAILED`)

**ניסוח מלא להצגה בסיכום (לא על גבי כפתור)**
- תשובה קצרה, מתומצתת ומעשית
- תשובה מקיפה, ארוכה ומורחבת

---

## State: publication_consent
**טקסט**
M: פעמים רבות השאלות שאנו נשאלים יכולות לסייע למתמודדים נוספים. אתה חשוב לנו ולעולם לא נפרסם את השאלה שלך אם לא תסכים.

את התשובות שאנו עונים יש לנו את הזכות לפרסם לתועלת נערים נוספים, בטשטוש פרטים מזהים.

האם אתה מאשר לנו לפרסם את שאלתך?

F: פעמים רבות השאלות שאנו נשאלים יכולות לסייע למתמודדות נוספות. את חשובה לנו ולעולם לא נפרסם את השאלה שלך אם לא תסכימי.

את התשובות שאנו עונים יש לנו את הזכות לפרסם לתועלת נערות נוספות, בטשטוש פרטים מזהים.

האם את מאשרת לנו לפרסם את שאלתך?

**כפתורים (בפועל בוואטסאפ)** — תווית קצרה ל־"טשטוש"; ב**סיכום** מוצג הניסוח המלא.
- אפשר לפרסם (`PUB_PUBLISH`)
- בטשטוש (`PUB_BLUR`)
- ללא פרסום (`PUB_NONE`)

**ניסוח מלא להצגה בסיכום (לא על גבי כפתור)**
- אפשר לפרסם
- פרסום בטשטוש פרטים מזהים
- לא לפרסום

---

## State: delivery_preference
**טקסט**
M: כיצד תרצה לקבל את התשובה?

F: כיצד תרצי לקבל את התשובה?

**כפתורים (בפועל בוואטסאפ)** — "שניהם" במקום ניסוח ארוך (מגבלת 20 תווים). ב**סיכום** עדיין מוצג "גם וואטסאפ וגם אימייל".
- וואטסאפ (`DELIV_WHATSAPP`)
- אימייל (`DELIV_EMAIL`)
- שניהם (`DELIV_BOTH`)

---

## State: collect_email (רק אם נבחר אימייל או גם-וגם)
**טקסט**
M: נא להזין כתובת אימייל לקבלת התשובה.

F: נא להזין כתובת אימייל לקבלת התשובה.

---

## State: email_invalid
**טקסט**
M: כתובת האימייל שהוזנה אינה תקינה.
נא להזין כתובת אימייל תקינה.

F: כתובת האימייל שהוזנה אינה תקינה.
נא להזין כתובת אימייל תקינה.

---

## State: confirm (סיכום מלא + עריכה/אישור)
**טקסט**
M: תודה.
להלן סיכום פרטי הפנייה:

מגדר: {{asker_gender}}
גיל: {{asker_age}}
כותרת: {{title}}
תוכן השאלה:
{{content}}

מסלול מענה: {{response_type}}
אפשרות פרסום: {{publication_consent}}
ערוץ קבלת תשובה: {{delivery_preference}}
{% if asker_email exists %}אימייל: {{asker_email}}{% endif %}

האם ברצונך לשנות או להוסיף משהו?
אם אין צורך בשינויים — נא לאשר את הפנייה.

F: תודה.
להלן סיכום פרטי הפנייה:

מגדר: {{asker_gender}}
גיל: {{asker_age}}
כותרת: {{title}}
תוכן השאלה:
{{content}}

מסלול מענה: {{response_type}}
אפשרות פרסום: {{publication_consent}}
ערוץ קבלת תשובה: {{delivery_preference}}
{% if asker_email exists %}אימייל: {{asker_email}}{% endif %}

האם ברצונך לשנות או להוסיף משהו?
אם אין צורך בשינויים — נא לאשר את הפנייה.

**כפתורים (בפועל בוואטסאפ)** — נשלחים ב־2 הודעות אינטראקטיביות (מגבלת 3 כפתורים להודעה). מסך “מה לשנות” מציג קטגוריות, ורק אחר כך מוצגות אפשרויות העריכה עצמן.
- פרטים אישיים (`EDIT_CAT_PERSONAL`)
- תוכן השאלה (`EDIT_CAT_CONTENT`)
- הגדרות תשובה (`EDIT_CAT_ANSWER`)
- ביטול פנייה (`CANCEL_REFERRAL`)
- סיום ואישור (`CONFIRM_DONE`)

**תפריטי משנה אחרי בחירת קטגוריה**
- `EDIT_CAT_PERSONAL`: שינוי מגדר (`EDIT_GENDER`) | שינוי גיל (`EDIT_AGE`)
- `EDIT_CAT_CONTENT`: שינוי שאלה (`EDIT_BODY`) | שינוי כותרת (`EDIT_TITLE`)
- `EDIT_CAT_ANSWER`: שינוי מסלול מענה (`EDIT_RESPONSE_TYPE`) | שינוי אפשרות פרסום (`EDIT_PUBLICATION_CONSENT`) |  שינוי ערוץ לתשובה (`EDIT_DELIVERY_PREFERENCE`)

---

## State: cancel_referral
**טקסט**
M: הפנייה בוטלה.\nכל מה שנאסף עד עכשיו לא ייכנס למערכת.\nהשיחה הסתיימה.

F: הפנייה בוטלה.\nכל מה שנאסף עד עכשיו לא ייכנס למערכת.\nהשיחה הסתיימה.

---

## State: edit_field_choice
*(אם אתם מפרידים state—אפשר להשתמש בטקסט ובכפתורים של `confirm`)*  

**כפתורים (בפועל בוואטסאפ)** — כמו ב־`confirm` למעלה.

---

## State: edit_gender
**טקסט**
M: נא להזין מחדש את המגדר באמצעות הכפתורים: `זכר` או `נקבה`.

F: נא להזין מחדש את המגדר באמצעות הכפתורים: `זכר` או `נקבה`.

**כפתורים (בפועל בוואטסאפ)**
- זכר
- נקבה

---

## State: edit_age
**טקסט**
M: נא להזין גיל במספרים.

F: נא להזין גיל במספרים.

---

## State: edit_body (שינוי תוכן השאלה)
**טקסט**
M: נא לכתוב את תוכן השאלה החדש.
ניתן לשלוח במספר הודעות.
לאחר שתשלח את התוכן הראשון ותסיים — יופיעו כפתורים לבחירה.

F: נא לכתוב את תוכן השאלה החדש.
ניתן לשלוח במספר הודעות.
לאחר שתשלח את התוכן הראשון ותסיימי — יופיעו כפתורים לבחירה.

**כפתורים (בפועל בוואטסאפ)**
- הוסף עוד
- סיימתי

---

## State: edit_title
**טקסט**
M: נא להזין כותרת חדשה לשאלה.
לאחר שתשלח את הכותרת הראשונה — יופיע כפתור `סיימתי`.
י
F: נא להזין כותרת חדשה לשאלה.
לאחר שתשלחי את הכותרת הראשונה — יופיע כפתור `סיימתי`.

**כפתורים (בפועל בוואטסאפ)**
- סיימתי

---

## State: edit_response_type
**טקסט**
M: באיזו רמת פירוט תרצה לקבל מענה?

F: באיזו רמת פירוט תרצי לקבל מענה?

**כפתורים (בפועל בוואטסאפ)** — זהים לשלב `response_type` הראשוני.
- קצר ולעניין (`RESP_SHORT`)
- מורחב (`RESP_DETAILED`)

---

## State: edit_publication_consent
**טקסט**
M: נא לבחור אפשרות לפרסום:

F: נא לבחור אפשרות לפרסום:

**כפתורים (בפועל בוואטסאפ)**
- אפשר לפרסם (`PUB_PUBLISH`)
- בטשטוש (`PUB_BLUR`)
- ללא פרסום (`PUB_NONE`)

**ניסוח מלא להצגה בסיכום (לא על גבי כפתור)** — כמו ב־`publication_consent`.
- אפשר לפרסם
- פרסום בטשטוש פרטים מזהים
- לא לפרסום

---

## State: edit_delivery_preference
**טקסט**
M: כיצד תרצה לקבל את התשובה?

F: כיצד תרצי לקבל את התשובה?

**כפתורים (בפועל בוואטסאפ)** — כמו ב־`delivery_preference`.
- וואטסאפ
- אימייל
- שניהם

---

## State: max_edits_reached (הגעתם למקסימום שינויים)
**טקסט**
M: הגעת למספר השינויים המקסימלי האפשרי.
באפשרותך לאשר את הפנייה כפי שהיא כעת.

F: הגעת למספר השינויים המקסימלי האפשרי.
באפשרותך לאשר את הפנייה כפי שהיא כעת.

**כפתורים (בפועל בוואטסאפ)**
- ביטול פנייה (`CANCEL_REFERRAL`)
- סיום ואישור (`CONFIRM_DONE`)

---

## State: waiting_admin_approval (אחרי יצירה/שליחה לאישור אדמין)
**טקסט**
M: תודה.
הפנייה שלך התקבלה והועברה למערכת המוקד.
בעז"ה נשלח לך תשובה בהמשך

F: תודה.
הפנייה שלך התקבלה והועברה למערכת המוקד.
בעז"ה נשלח לך תשובה בהמשך

---

## State: extra_inbox_message_while_waiting (נשלחה הודעה נוספת בזמן waiting_admin_approval)
**טקסט**
M: קיבלנו הודעה נוספת מהשואל.
היא תועבר לעיון הנציג האנושי.
לא נבצע עדכונים אוטומטיים עד לסיום הבדיקה והאישור.

F: קיבלנו הודעה נוספת מהשואל.
היא תועבר לעיון הנציג האנושי.
לא נבצע עדכונים אוטומטיים עד לסיום הבדיקה והאישור.

---

## State: start_new_process_while_waiting (פתיחה מחדש של תהליך מלא)
**טקסט**
M: פתחנו פנייה חדשה.
הפנייה הקודמת עדיין נמצאת בבדיקה במערכת.

F: פתחנו פנייה חדשה.
הפנייה הקודמת עדיין נמצאת בבדיקה במערכת.

---

## הערה טכנית (למפתחי ה-FSM)
- מומלץ לייצר מזהה הודעה/קלט (`provider_message_id`) ולמנוע עיבוד כפול.
- בזמן `waiting_admin_approval` לפי הסיכום הנוכחי:
  - אם זו לא "שאלה חדשה" אלא בקשת שינוי במסגרת ההמתנה: לא מעדכנים אוטומטית, רק מציגים לאדמין דרך Inbox.

